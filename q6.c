//Use math library (pow(), sqrt(), etc.).

#include <stdio.h>
#include <math.h>

int main()
{
    //Sqrt
    double A;
    A = sqrt(9);
    printf("The sq root of 9 is %lf\n",A);

    //pow
    double B;
    B = pow(2,3);
    printf("The power of 2 to 3 is %lf\n",B);
